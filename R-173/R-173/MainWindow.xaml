<Window x:Class="R_173.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:R_173"
        xmlns:views="clr-namespace:R_173.Views"
        mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="1024"
        Title="Р-173" WindowState="Maximized" FontFamily="Times New Roman">
    <DockPanel>
        <UniformGrid DockPanel.Dock="Top" Columns="4">
            <UniformGrid.Resources>
                <Style TargetType="RadioButton">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="border" Margin="2" CornerRadius="3, 3, 0, 0" Background="WhiteSmoke">
                                    <TextBlock Margin="3" 
                                           Text="{TemplateBinding Content}"
                                           FontSize="18" FontFamily="Times New Roman" FontWeight="Bold" 
                                           TextAlignment="Center"
                                           Cursor="Hand"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="Khaki"/>
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="OliveDrab"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </UniformGrid.Resources>

            <RadioButton Grid.Column="0" GroupName="Tabs" Content="1. Назначение, ТТХ, комплект" Click="Button_Click" CommandParameter="{x:Type views:Appointment}"/>
            <RadioButton Grid.Column="1" GroupName="Tabs" Content="2. Обучение" Click="Button_Click" CommandParameter="{x:Type views:Training}"/>
            <RadioButton Grid.Column="2" GroupName="Tabs" Content="3. Работа на радиостанции" Click="Button_Click" CommandParameter="{x:Type views:Work}"/>
            <RadioButton Grid.Column="3" GroupName="Tabs" Content="4. Задачи" Click="Button_Click" CommandParameter="{x:Type views:Tasks}"/>
        </UniformGrid>

        <ContentControl x:Name="MainContent"/>
    </DockPanel>
</Window>
